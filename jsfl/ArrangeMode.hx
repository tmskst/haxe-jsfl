package jsfl;

// 指定できる値は、「back」、「backward」、「forward」および「front」です。

@:enum 
abstract ArrangeMode(String) {

	var BACK     = "back";
	var BACKWARD = "backward";
	var FORWARD  = "forward";
	var FRONT    = "front";

}
